#include <studio,h>

-struct studen {
    char name[50];
    int age;
    char usn[20];
    float height ;
    float wheigh 
    char collage [100
};

-int main() {
    struct student s
    
   
    printf("enter student name:");
    fgets(s.name,sizeof(s.name).stin);

   printf("enter age:");
   scanf("%d",&s.age);
}
